import React, { memo } from 'react'

import { PlayerWrapper } from './style'

function Player() {

  return (
    <PlayerWrapper>
      Player
    </PlayerWrapper>
  )
}


export default memo(Player)
